apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgres-nonproduction
  namespace: argocd
spec:
  project: postgresql-platform
  source:
    repoURL: https://github.com/robbertvdzon/robberts-infrastructure.git
    targetRevision: main
    path: manifests/postgresql/nonproduction
  destination:
    server: https://kubernetes.default.svc
    namespace: postgres-nonproduction
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
    - CreateNamespace=true

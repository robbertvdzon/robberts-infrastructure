apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - serviceaccount.yaml
  - rolebinding-scc.yaml
  - configmap-server.yaml
  - deployment.yaml
  - service.yaml
# namespace.yaml staat niet in resources: de Application maakt de namespace
# zelf aan via CreateNamespace=true (zelfde patroon als smb-timemachine).
# Het bestand blijft bestaan als documentatie van de labels.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - serviceaccount.yaml
  - rolebinding-scc.yaml
  - sealed-secret-credentials.yaml
  - deployment.yaml
# namespace.yaml expres NIET hier: deze ArgoCD-instance draait "namespaced
# mode" en mag geen cluster-scoped Namespace-object beheren ("cluster level
# Namespace ... can not be managed when in namespaced mode" — gebleken tijdens
# het live inschakelen van deze Application). Namespace aanmaken + labelen is
# daarom, net als bij youtrack, een eenmalige handmatige prereq — zie
# argocd-application.yaml.
